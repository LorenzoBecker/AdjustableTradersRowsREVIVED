# Changelog

## 1.0.1

- Updated/reconstructed source for SPT 4.x compatibility.
- Keeps the mod client-side and UI-only.
- Uses a runtime UI scanner to apply the configured trader-card column count to the active trader grid.
- Adds config options for column count, scan interval, and debug logging.
